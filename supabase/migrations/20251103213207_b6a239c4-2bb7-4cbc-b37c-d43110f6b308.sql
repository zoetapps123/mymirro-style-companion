-- Add image URLs to battles participants JSONB field
-- This allows storing image URLs alongside participant metadata for history display

COMMENT ON TABLE battles IS 'Outfit battle records with participant images and metadata';
